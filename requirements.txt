# ============================================================
# Elysia Personal Voice Assistant — requirements.txt
# System deps (install terlebih dahulu via OS package manager):
#   sudo apt install ffmpeg portaudio19-dev
# ============================================================

# --- Audio I/O ---
sounddevice>=0.5,<1.0
numpy>=2.0,<3.0

# --- Wake Word ---
pvporcupine>=4.0,<5.0
# openwakeword is the default engine (offline, no key). Pulls onnxruntime,
# scikit-learn, scipy and tqdm automatically. Pinned to 0.4.x for the
# wakeword_model_paths API used by audio/wake_word.py.
openwakeword>=0.4,<0.5

# --- VAD / Endpointing ---
# Silero VAD membutuhkan torch (CPU-only cukup)
torch>=2.0,<3.0
torchaudio>=2.0,<3.0
# Model Silero VAD di-download via torch.hub saat startup

# --- Speech-to-Text (STT) ---
faster-whisper>=1.1,<2.0

# --- LLM & Orkestrasi ---
google-genai>=2.20,<3.0

# --- Text-to-Speech (TTS) ---
edge-tts>=7.0,<8.0
piper-tts>=1.2,<2.0

# --- Resilience & Retry ---
tenacity>=9.0,<10.0

# --- Configuration Management ---
pydantic-settings>=2.10,<3.0

# --- Structured Logging ---
structlog>=26.0,<27.0

# --- Dev / Testing ---
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<2.0
httpx>=0.27,<1.0
